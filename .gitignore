# Claude Cowork — worktrees y archivos internos
.claude/

# Scripts de generación — solo corren localmente, no se sirven en web
papers/*.py
papers/__pycache__/
papers/__pycache__zoove-travel/
zoopedia-audit.py
zoopedia-audit-report.html

# Archivos de prueba generados durante desarrollo
papers/art08-test.pdf
papers/brand_new_*.pdf
papers/copy_new_*.pdf
papers/test_*.pdf

# Fotos originales sin procesar (nunca publicar)
*.jfif
WhatsApp Image*

# Vigía (uso interno, no se publica)
vigia/

# Fotos originales sin procesar (pesadas, SIN metadatos de copyright).
# Solo se publican las versiones WebP envenenadas en images/viajes/
fotos_blog_usar_y_descartar/
fotos_blog_*/
viaja-con-tu-mascota-fotos/

# Carpetas de trabajo interno
seleccionar por ocr y cloudvision/
**/__pycache__/
*.pyc

# ─────────────────────────────────────────────────────────────
# BASURA QUE NUNCA SE PUBLICA (se coló con un `git add .` el 12-jul-2026)
# Backups, borradores y plantillas. Si se suben, Google los indexa
# como contenido duplicado de los artículos reales.
# ─────────────────────────────────────────────────────────────
_backup_*/
**/_backup_*/
_draft-*
_template_*
**/_template_*
PREVIEW_*
original_index.html
indexnow-submit.html

# ─────────────────────────────────────────────────────────────
# INTERNO — NUNCA SE SIRVE EN LA WEB (limpieza 12-jul-2026)
# Estaba todo descargable: el método (CLAUDE.md, PROMPT-MAESTRO),
# las memorias de sesión, las proyecciones SEO, los borradores de
# papers y las fotos originales de clientes sin metadatos.
# ─────────────────────────────────────────────────────────────
*.py
*.md
!README.md
*.docx
*.xlsx
*.ps1
*.bat
*.mdc
*.bak
*.tmp

# KB privada de referencias (QIDs Wikidata, ROR, etc.) — NUNCA se publica.
# Ya cubierto por *.md, pero explícito por claridad. Consultar antes de buscar.
_referencias-privadas.md
desktop.ini
.cursor/
docs/
articulos-interes/ARTICLE_AUDIT_V10/
scripts/auditoria/
inventario_zoovet.json
indexnow-payload*.json
urls

# Excepciones: esto SÍ lo necesita la web (no ignorar)
!scripts/search_data.js
!index.json
!planificador-viaje-mascota/rules.json
!favn-data/index.json

# ─────────────────────────────────────────────────────────────
# SECRETOS — jamás al repo (defensa en profundidad, 18-jul-2026)
# El sitio es HTML estático y hoy NO hay ningún secreto rastreado (verificado).
# Esto blinda contra subir por error una credencial en el futuro.
# NO afecta a los .json que la web sí necesita: sus excepciones con '!' están arriba.
# ─────────────────────────────────────────────────────────────
.env
.env.*
*.env
!.env.example
*.key
*.pem
*.pfx
*.p12
*.keystore
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.ppk
.netrc
.npmrc
.pypirc
# credenciales en JSON (los .json públicos ya están permitidos con '!' arriba)
*secret*.json
*secrets*.json
*credential*.json
*credentials*.json
service-account*.json
*serviceaccount*.json
firebase-adminsdk*.json
gha-creds-*.json
auth.json
token.json
*.token
# nubes / infraestructura / estado
.aws/
.gcloud/
gcloud-*.json
*.tfstate
*.tfstate.*
# genéricos
secrets.*
*.secret
credentials.*
config.local.*
*.local.json

tentativas/
